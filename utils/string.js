// MDN 권장 패턴: 특수문자에 백슬래시를 붙여 이스케이프한다.\nexport const escapeRegExp = (str = \"\") => str.replace(/[.*+?^${}()|[\\]\\\\]/g, \"\\\\$&\");\n\nexport const replaceVars = (template, variables) => {\n  let out = template || \"\";\n  Object.entries(variables || {}).forEach(([k, v]) => {\n    const safeKey = escapeRegExp(String(k).trim());\n    // 공백은 `\\\\s*`로, 중괄호는 리터럴로 매칭\n    const pattern = new RegExp(`{{\\\\s*${safeKey}\\\\s*}}`, \"g\");\n    out = out.replace(pattern, String(v ?? \"\"));\n  });\n  return out;\n};